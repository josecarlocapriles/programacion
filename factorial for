#include "stdafx.h"
#include <iostream>
#include <conio.h>
using namespace std;

void main()
{
	int factorial, i, n;
	factorial = 1;
	do {
		cout << "ingrese n: ";
		cin >> n;
	} while (n<0);
	for (i=2;i<=n;i++)
		factorial = factorial * i;
	cout << "el factorial es: " << factorial;
	getch(); 
}
